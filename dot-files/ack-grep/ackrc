# CMake build
--ignore-dir=build
--ignore-dir=cmake-build-debug

# Bazel build
--ignore-dir=bazel-*
--ignore-dir=.cache

# Other files & directories.
--ignore-dir=kodiak_python_env
--ignore-file=is:compile_commands.json
